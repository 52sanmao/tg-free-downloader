# Telegram API credentials
# Get yours at https://my.telegram.org/apps
# These are only needed for CI builds. The app also allows setting
# credentials at first launch via the UI (recommended for end users).
VITE_API_ID=your_api_id_here
VITE_API_HASH=your_api_hash_here
